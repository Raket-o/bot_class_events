from loader import dp

from config_data.config import DEFAULT_COMMANDS


def set_default_commands(dp):

    # bot.set_my_commands(DEFAULT_COMMANDS)
    # bot.set_my_commands(
    #     [BotCommand(*i) for i in DEFAULT_COMMANDS]
    # )
    pass


# Запросить минимальные значения (команда /low).
# Запросить максимальные значения (команда /high).
# Запросить диапазон значений (команда /custom).
# Узнать историю запросов (команда /history).